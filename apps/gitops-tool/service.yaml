apiVersion: v1
kind: Service
metadata:
  name: gitops-tool
  namespace: default
  labels:
    app: gitops-tool
spec:
  type: LoadBalancer
  selector:
    app: gitops-tool
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
